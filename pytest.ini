[pytest]
# Verification configuration (NASA-STD-8739.8 software assurance).
# -W error promotes warnings to failures so the suite enforces a
# zero-warning policy (Power of Ten Rule 10: compile/run clean).
testpaths = tests
addopts = -W error
